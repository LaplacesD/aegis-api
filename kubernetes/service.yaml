apiVersion: v1
kind: Service
metadata:
  name: aegis-api
  labels:
    app: aegis-api
spec:
  type: ClusterIP
  selector:
    app: aegis-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      name: http
